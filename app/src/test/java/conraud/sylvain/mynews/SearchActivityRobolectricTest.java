package conraud.sylvain.mynews;

import android.widget.Button;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.Robolectric;
import org.robolectric.RobolectricTestRunner;
import org.robolectric.annotation.Config;

import conraud.sylvain.mynews.ui.activity.MainActivity;
import conraud.sylvain.mynews.ui.activity.SearchActivity;

import static junit.framework.TestCase.assertNotNull;
import static junit.framework.TestCase.assertTrue;

@RunWith(RobolectricTestRunner.class)

public class SearchActivityRobolectricTest {



/*
    @Test
    public void clickButton() {
       SearchActivity activity = Robolectric.setupActivity(SearchActivity.class);

        Button button =  activity.findViewById(R.id.button_search);
        assertNotNull("test button could not be found", button);
        assertTrue("button does not contain text 'Click Me!'", "Search".equals(button.getText()));
    }
*/
}
